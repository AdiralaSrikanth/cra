import React from 'react'


class List extends React.Component{
    render(){
        return (
            <div>
    {/* <h2>List Component- {1+1} {getFriends()}</h2>
        
        {friends.map((list, index)=>{
            return(
                <li key={index}>{list}</li>
            )
        })} */}
            </div>
        )
    }
}

export default List